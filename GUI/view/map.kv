#:kivy 2.2.1
#:import Factory kivy.factory.Factory
#:import AddObjectPopup utils.add_object_popup.AddObjectPopup
#:import MultiSelectDropdown utils.multi_select_dropdown.MultiSelectDropdown

<AddObjectPopup>:
    id: add_object_popup
    auto_dismiss: False
    title: "Добавить объект на карту"
    size_hint: 0.5, 0.5
    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        GridLayout:
            cols: 2
            Button:
                text: "Добавить"
                on_release: root.add_object()
            Button:
                text: "Отмена"
                on_release: root.dismiss()

<MultiSelectOption@ToggleButton>:
    size_hint: 1, None
    height: '48dp'

<MainScreen>:
    BoxLayout:
        orientation: "horizontal"

        BoxLayout:
            orientation: "vertical"
            Button:
                text: "wanna see my ids?"
                on_release: print(root.ids)
            Button:
                text: "+"
                size_hint: 1, 1
                on_release: Factory.AddObjectPopup().open()
            MultiSelectDropdown:
                id: map_layers_dropdown
                values: root.ids.map_view.layers

        Splitter:
            sizable_from: "left"

            BoxLayout:
                orientation: "vertical"
                ScatterLayout:
                    StencilView:
                        MapView:
                            id: map_view
                            selected_layers: root.ids.map_layers_dropdown.selected_values

<MapView>:
    do_rotation: False
    on_touch_down: print(self.to_local(*args[1].pos), (self.size))
